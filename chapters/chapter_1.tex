\chapter{Background \& Related Work}
\label{chp:background}

We first review how drones navigate indoors without GPS, and locate the gap this thesis closes. We then introduce the drone platform whose limited budget shapes the design. We follow with the visible-light theory the later chapters rely on. We close with the navigation algorithms of Huang et al.~\cite{huang2026lta} that this work extends, and the simulator in which it is developed.

\section{Related Work}
\label{sec:related-work}

Small unmanned aircraft increasingly perform tasks once carried out by people, such as inspecting structures, monitoring the environment, and tracking inventory in warehouses~\cite{floreano2015drones}. Many of these tasks take place indoors, and the indoor inspection drone market alone is forecast to grow from USD~254~million in 2024 to USD~614~million in 2032~\cite{intelmarket2026}. Indoors, a drone cannot rely on GPS, which is blocked or too coarse to navigate by~\cite{zafari2019indoor}. It must instead find its heading or position from other signals, either carried on board or already present in the space.

Indoor navigation methods differ in the signal they use, and we compare them by two costs. One cost falls on the drone: the sensing and computation it must carry, which exceeds a micro-drone's capacity. The other falls on whoever deploys the system: the infrastructure that must be put in place before the drone can fly, which decides how easily the method can be adopted. Table~\ref{tab:methods-comparison} summarises the main approaches against these two costs.

One family of methods locates the drone against infrastructure prepared in the environment beforehand. Two of these use radio. Ultra-wideband anchors measure how long a pulse takes to travel between an anchor and the drone, which places the drone to within tens of centimetres~\cite{queralta2020uwb}. WiFi fingerprinting instead matches the signal strengths the drone measures against a map of measurements surveyed in advance, which localises to within about a metre~\cite{zafari2019indoor}. WiFi therefore covers a building using existing hardware, whereas ultra-wideband is more accurate but needs dedicated anchors. A third method uses sound: acoustic positioning places the drone from the time ultrasonic pulses take to travel from fixed beacons~\cite{sun2025acoustic, altena2023acoustic}. All three share one cost in our setting: they require infrastructure installed or surveyed before the first flight.

Vision gets rid of anchors by having the drone build its own reference as it flies. A camera captures a stream of images to process. Visual simultaneous localisation and mapping, or visual SLAM, then tracks features across those images, and from how they move it estimates both the drone's path and a map of its surroundings~\cite{murartal2017orbslam2}. This needs no infrastructure in the space. The cost falls on the drone instead: visual SLAM needs a capable camera and enough on-board computation to process the images in real time, which a micro-drone with little payload and a tight power budget cannot provide.

Each of these approaches is unsuitable for a small drone in one of two ways. Radio, acoustic, and dense visible-light positioning add infrastructure to every space, while vision adds a camera and computation the drone cannot carry. The gap is a method that needs neither, and light provides one. The spaces a drone works in are already lit, so an ordinary lamp can become a navigation beacon once it is modulated, switched on and off in a pattern a light sensor can detect, and nothing is added to the room but the modulation itself~\cite{pathak2015vlc}. The drone then carries only a light sensor, and reading that sensor is well within its onboard processor's budget.

Visible-light positioning is the most accurate light-based method. It triangulates a photodiode-equipped receiver from several modulated ceiling luminaires, each identified by its own modulation, and reaches centimetre-level accuracy indoors~\cite{kuo2014luxapose, zhuang2018vlp}. Triangulation needs at least three sources in view at once, so the method assumes a dense, fixed grid of known lights. That grid is costly to provide. The luminaires must be installed where their light overlaps and surveyed to precise known positions. Many spaces cannot arrange their lighting so regularly, as street or aisle lighting shows. The accuracy is real, but it depends on exactly the dense infrastructure we set out to avoid.

A single beacon, or light, places a far smaller demand on the space. With only one source a drone cannot triangulate a position, since triangulation needs at least three. It can, however, recover the direction of the light, which is sufficient to navigate toward it. This is a deliberately minimal design, not a compromise: it keeps the existing-infrastructure advantage of light, adds only the modulation, and requires nothing of the drone beyond a light sensor. Huang et al.\ demonstrate such single-beacon navigation on a micro-drone~\cite{huang2026lta}.

\begin{table}[ht]
    \centering
    \caption{Indoor navigation approaches, set against two costs: the infrastructure that must be installed in the space, and the sensing and computation the drone must carry. The single light beacon, the approach this thesis builds on, keeps both small.}
    \label{tab:methods-comparison}
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{p{3.3cm} p{3.8cm} p{3.8cm}}
        \hline
        Approach & Infrastructure to install & On-board sensing and compute \\
        \hline
        Ultra-wideband                  & Dedicated radio anchors        & Radio transceiver \\
        WiFi fingerprinting             & Prior site survey              & WiFi radio \\
        Acoustic                        & Ultrasonic anchors             & Microphone \\
        Visual SLAM                     & None                           & Camera and heavy computation \\
        Dense visible-light positioning & Grid of modulated luminaires   & Photodiode \\
        Single light beacon             & One modulated lamp             & Photodiode \\
        \hline
    \end{tabular}
\end{table}

Recovering a direction, rather than a position, from an array of sensors is an established idea. In radio, amplitude-comparison direction finders estimate a transmitter's bearing by comparing its strength across several differently pointed antennas~\cite{iqbal2020amplitude}. Single-beacon light navigation applies the same principle, with photodiodes and light in place of antennas and radio.

These single-beacon methods reach one beacon in open space, but no further. Two problems remain. An obstacle between the drone and the beacon blocks the light, and the methods offer no way around it. A mission that must visit several beacons in turn is also beyond them, since they track only one light at a time. This thesis solves both, with a custom sensing board and a controller that builds and follows a map of the light field. We describe the methods it builds on in Section~\ref{sec:algorithms}.

\section{Drone Platform}
\label{sec:platform}

The platform for this thesis is the Bitcraze CrazyFlie~2.1, an open-source research quadrotor of about 27~grams that is small enough to be held in one hand~\cite{crazyflie21}. We chose it because it is openly programmable and built around an expansion-deck interface, so we can mount a custom sensing board and run our own controller in its firmware. It is also widely used in micro-drone research, which makes the results easier to compare and reproduce.

What defines the platform for our purposes is its limited payload capacity. The CrazyFlie can localise itself accurately with the Lighthouse positioning deck, which tracks infrared base stations placed around the room~\cite{lighthousedeck}. That option, like the radio and dense visible-light methods of Section~\ref{sec:related-work}, depends on infrastructure installed and calibrated before the first flight. Our aim is navigation that adds nothing to the space, so we set such methods aside and rest on light sensing alone.

The mass budget that results is small. The CrazyFlie~2.1 lifts roughly 15~grams of payload, of which the FlowDeck~V2, a downward-facing camera that gives the drone its odometry, takes about 2~grams, leaving on the order of 13~grams for the sensing hardware this thesis adds~\cite{crazyflie21, flowdeckv2}. That hardware must also fit within the drone's 44~millimetre frame and draw only the current available from its small battery. These limits set the requirements the photodiode board of Chapter~\ref{chp:hardware} is designed to meet, and the later chapters build the light map on the drone's odometry.

\section{Visible Light Communication \& Sensing}

Visible light communication modulates a light faster than the eye can perceive~\cite{pathak2015vlc}. A person sees an ordinary, steady light, but a photodiode sampling it quickly enough reads the modulation as a signal. In this work each beacon is a light switched on and off at a fixed frequency, which is enough to distinguish it from steady background light and from beacons at other frequencies.

How strongly a beacon appears at the drone depends on more than distance. Two angles also matter: the angle at which the light leaves the source, and the angle at which it meets the photodiode. Each contributes a cosine factor, and the intensity falls with the square of the distance, a relationship known as the Lambertian model~\cite{komine2004vlc}. Figure~\ref{fig:lambertian} shows the geometry. Writing $\alpha$ for the angle off the source's axis, $\beta$ for the angle of incidence at the sensor, and $d$ for the distance, the received intensity is proportional to $\cos^{m}\!\alpha \, \cos\beta / d^{2}$, where the order $m$ describes how narrowly the source concentrates its light. Intuitively, the light weakens as either the source or the sensor turns away from the line between them, and the order $m$ sets how sharply the source's contribution falls off, higher for a more tightly focused source.

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}[>=Latex]
        \coordinate (S) at (0,3.4);
        \coordinate (Ax) at (0,0);
        \coordinate (P) at (3.4,0.5);
        \coordinate (N) at (3.4,2.0);
        \fill (S) circle (2pt);
        \node[above] at (S) {light source};
        \draw[dashed] (S) -- (Ax);
        \draw[->] (S) -- (P);
        \node[above right] at ($(S)!0.5!(P)$) {$d$};
        \pic[draw, angle radius=0.95cm, "$\alpha$"] {angle = Ax--S--P};
        \fill (P) circle (2pt);
        \node[below] at (P) {photodiode};
        \draw[dashed] (P) -- (N);
        \pic[draw, angle radius=0.75cm, "$\beta$"] {angle = N--P--S};
    \end{tikzpicture}
    \caption{The Lambertian geometry. The light leaves the source at an angle $\alpha$ off its axis, travels a distance $d$, and meets the photodiode at an angle $\beta$ off the sensor's normal. The received intensity is proportional to $\cos^{m}\!\alpha\,\cos\beta/d^{2}$.}
    \label{fig:lambertian}
\end{figure}

This dependence on angle as well as distance makes a single intensity reading ambiguous. A strong reading can mean a nearby source seen from the side or a distant source seen head-on, and one photodiode cannot tell the two apart, as Figure~\ref{fig:ambiguity} illustrates. This ambiguity is the reason one light cannot fix an absolute position, and the reason positioning systems combine many overlapping sources. We avoid this problem by treating the light as a direction to follow rather than a position to solve for.

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}[>=Latex]
        \coordinate (P) at (0,0);
        \fill (P) circle (2pt);
        \node[below] at (P) {photodiode};
        \draw[dashed] (P) -- (0,1.5) node[above] {normal};
        \coordinate (A) at (-1.8,1.5);
        \coordinate (B) at (0.6,3.1);
        \fill (A) circle (2pt);
        \fill (B) circle (2pt);
        \draw[->] (A) -- (P);
        \draw[->] (B) -- (P);
        \node[left, align=center] at (A) {near,\\off-axis};
        \node[right, align=center] at (B) {far,\\head-on};
        \node[align=center, font=\small] at (3.0,1.3) {both give the\\same reading};
    \end{tikzpicture}
    \caption{The intensity ambiguity. A near source seen well off the sensor's axis and a far source seen head-on can deliver the same reading to a single photodiode, so one reading does not fix the source's position.}
    \label{fig:ambiguity}
\end{figure}

\subsection{The Light Signal}
\label{sec:lightsignal}

The drone senses this light with photodiodes. A photodiode produces a small electrical current proportional to the light striking it, and the board converts that current into a value. The navigation reads one value per photodiode, which we call the per-sensor light signal and write $S_n$ for sensor $n$. It measures how strongly that sensor sees the beacon. How $S_n$ is recovered differs between the two settings this thesis uses. On the real drone it is extracted from a noisy voltage, as Chapter~\ref{chp:realworld} describes, and in simulation it is read directly from a modelled sensor, as Section~\ref{sec:simulation} describes. For the algorithms that follow, it is enough that each sensor reports one value for how strongly it sees the beacon.

\section{Algorithms from Huang et al.}
\label{sec:algorithms}

This thesis builds on the single-beacon algorithms of Huang et al.~\cite{huang2026lta}. They propose three: bearing angle guidance (BAG), dither-free gradient ascent (DGA), and dither extremum seeking (DES). We build on BAG and DGA. DES we describe only to set it aside. Table~\ref{tab:algorithms-comparison} compares all three.

\subsection{Bearing Angle Estimation}
\label{sec:bearing}

Bearing angle guidance recovers the direction of the light from an array of photodiodes~\cite{huang2026lta}. Each photodiode in the array faces a different, known direction, and responds most strongly to light arriving from where it faces. When a beacon lies to one side, the photodiodes facing it report a stronger signal than those facing away. Comparing the photodiodes against one another therefore reveals the direction of the beacon.

The bearing combines these per-sensor readings into a single direction. Each photodiode contributes a vector pointing the way it faces, weighted by how strongly it sees the beacon, and the vectors are summed. Figure~\ref{fig:bearing-vectorsum} shows this for a ring of eight photodiodes. Using the per-sensor light signal $S_n$ and writing $\theta_n$ for the direction sensor $n$ faces, the bearing $\psi_B$ is the angle of the weighted sum,
\begin{equation}
\psi_B = \operatorname{atan2}\!\left( \sum_{n} S_n \sin\theta_n,\; \sum_{n} S_n \cos\theta_n \right).
\label{eq:bearing}
\end{equation}
The sensor facing the beacon contributes the most, so the sum points toward the strongest light. Spacing eight photodiodes evenly around the ring covers every direction in the horizontal plane, so whichever way the beacon lies, at least one sensor faces it. The result is a direction in the plane the photodiodes lie in.

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}[>=Latex]
        \foreach \a in {0,45,...,315} {
            \draw[->, gray!55, thin] (0,0) -- (\a:0.8);
        }
        \draw[->, thick] (0,0) -- (0:0.9);
        \draw[->, thick] (0,0) -- (45:1.3);
        \draw[->, thick] (0,0) -- (90:0.65);
        \draw[->, very thick, red] (0,0) -- (35:1.8) node[midway, above left] {$\psi_B$};
        \fill[orange] (35:2.4) circle (3pt) node[right=2pt] {beacon};
        \begin{scope}[shift={(-3.0,-2.6)}]
            \draw[->, gray!55, thin] (0,0.6) -- (0.6,0.6) node[right, black, font=\footnotesize] {sensor direction};
            \draw[->, thick] (0,0.2) -- (0.6,0.2) node[right, font=\footnotesize] {reading $S_n$};
            \draw[->, very thick, red] (0,-0.2) -- (0.6,-0.2) node[right, black, font=\footnotesize] {bearing $\psi_B$};
        \end{scope}
    \end{tikzpicture}
    \caption{The bearing as a magnitude-weighted vector sum on a ring of eight photodiodes. The grey arrows are the directions the sensors face; the black arrows are their readings, longer where the sensor sees the beacon more strongly; their sum, in red, is the bearing $\psi_B$, which points toward the beacon.}
    \label{fig:bearing-vectorsum}
\end{figure}

BAG is immediate and accurate. From a single set of readings it points at the beacon, with no need to move or explore, which makes it a strong reactive heading~\cite{huang2026lta}. Its weakness is that it has no memory. Each estimate uses only the latest readings, so the drone steers toward whatever light is strongest at that instant, even when that light is a reflection. The readings alone do not record the space the drone has crossed, so the bearing cannot tell that an obstacle stands between the drone and the beacon. Steering on the bearing alone, the drone heads directly toward the strongest light and cannot plan a path around an obstruction.

\subsection{Gradient-Based Source Seeking}

Where the bearing is reactive and memoryless, gradient-based source seeking lets the drone use the history of what it has sensed. It treats the light as a field that grows toward the beacon, and moves in the direction it estimates the field increases fastest. Following that gradient leads to the peak of the field, which for a light field is the beacon itself. Because it is built from measurements gathered across a region, the gradient still gives a direction when the beacon is briefly out of direct view.

The difficulty is measuring the gradient, and Huang et al.\ propose two ways to do it~\cite{huang2026lta}. The first, dither extremum seeking, uses a single photodiode and adds a small oscillation, or dither, to the drone's path. As the drone oscillates, it measures how the light changes and infers the uphill direction. This is the classical extremum-seeking approach to source seeking without knowledge of the drone's own position~\cite{ariyur2003extremum, zhang2006sourceseeking, cochran2009sourceseeking}. Its drawback is the dither itself. The constant oscillation produces indirect, inefficient flight, and the probing motion is easily overwhelmed by disturbances. This thesis does not use it.

The second, dither-free gradient ascent, removes the oscillation. It still uses a single photodiode, but instead of probing it estimates the gradient from the drone's recent motion. The drone records where it has been and how strong the light was there, then fits a plane to those recent measurements by least squares, and the slope of that plane is the gradient~\cite{huang2026lta}. This avoids the dither, but it depends on a single sensor and on position estimated by dead reckoning, by accumulating the drone's own motion with no external reference, so the estimate drifts over time. It is therefore slow, since the drone must keep moving to gather points, and sensitive to that drift. This dither-free estimate is the gradient method this thesis builds on. Chapter~\ref{chp:algorithm} shows how estimating the gradient from all eight photodiodes and a map of the field, rather than from one sensor and recent motion, removes some of these weaknesses.

\subsection{Comparing the Algorithms}

Table~\ref{tab:algorithms-comparison} sets the three methods side by side, with the path length and travel time Huang et al.\ measured for each on the same task~\cite{huang2026lta}. The bearing reaches the source most directly, in 7.3~metres and 8.4~seconds, at the cost of the full photodiode array. Both single-photodiode methods are markedly slower. Dither-free ascent takes 8.6~metres and 21.4~seconds, and the dithering extremum seeker is the slowest and least direct at 12.2~metres and 25.6~seconds, its oscillation lengthening the path. The bearing and the dither-free gradient are the two this thesis builds on, and Chapter~\ref{chp:algorithm} develops how they are combined.

\begin{table}[ht]
    \centering
    \caption{The three single-beacon navigation algorithms of Huang et al.~\cite{huang2026lta}, with the path length and travel time each took to reach the source on the same task in their evaluation. This thesis builds on the bearing and the dither-free gradient.}
    \label{tab:algorithms-comparison}
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{p{2.4cm} p{2.7cm} p{2.7cm} p{2.7cm}}
        \hline
         & Bearing angle guidance & Dither extremum seeking & Dither-free gradient ascent \\
        \hline
        Sensing          & Photodiode array       & Single photodiode             & Single photodiode \\
        Path length (m)  & 7.3                    & 12.2                          & 8.6 \\
        Travel time (s)  & 8.4                    & 25.6                          & 21.4 \\
        Main weakness    & No memory of the field & Inefficient oscillating flight & Slow; depends on dead reckoning \\
        \hline
    \end{tabular}
\end{table}

\section{Simulation Environment}
\label{sec:simulation}

The controller is developed in simulation before it is flown on real hardware. Simulation runs a scenario repeatedly under identical conditions, and lets a controller fail without risking the drone. This makes it well suited to designing and comparing the navigation controllers of this thesis.

We use Webots, an open-source robotics simulator~\cite{webots}, with the model that Huang et al.\ built for their work~\cite{huang2026lta}. The model reproduces the flight of their lighter-than-air blimp and the light its sensors receive. Huang et al.\ validated the flight dynamics against published aerodynamic data and real flights, so we take the physical behaviour as given.

The light sensing in the model is deliberately simplified. Webots can render light by ray tracing, but doing so for a fast-modulated source is expensive and unnecessary here, since we study the navigation rather than the optics. Each photodiode is therefore a Webots light sensor that reads the intensity reaching it directly from the modelled source, and the per-sensor light signal $S_n$ is taken straight from this reading.
